spring.application.name=backend

# This tells Spring Boot to get the database URL from the deployment server's environment
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA/Hibernate Configuration for PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# This tells Spring Boot to get the JWT secret from the environment for better security
application.security.jwt.secret-key=${JWT_SECRET}
application.security.jwt.expiration=86400000

# --- EMAIL CONFIGURATION (This is correct) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=contact.kishanraj@gmail.com
spring.mail.password=atjk jqzg spcm wngl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true